package model;

public class Password {

 Private final static int LONG DEF = 8;
 private int longitud
 private String contraseña;




//Getters & Setter

public int getlongitud(
    return longitud

public void setLongitud(int longitud)
this.longitud = longitud;
public string getContrasenia

)}
//Constructores
public Password(){
    this(LONG_DEF);

    public Password(int longitud){
        this.longitud = longitud;
        this.contraseña = generaContrasena();
    }

    //Metodos
    public String generaContrasenia(){
        String password = "";

        for (int i = 0; i < longitud.length; i++) {
            int eleccion = (int)Math.floor(Math.random()*3+1));

            if (eleccion == 1){
                character minuscula = (char)(ing)Math.floor(Math.random)()*(26)+65));
                password += minuscula;
            } else if(eleccion == 2){
                character mayuscula = (char)(int)Math.floor(Math.random()*(26)+65));
            }else{            
                character numero = (char)(int)Math.floor(Math.random)()*(10)+48));
        }
       password += character;
    }
        return password;
        
        public boolean esFuerte(){
            int cuentaNumero = 0;
            int cuentaMinuscula = 0;
            int cuentaMayusculas = 0;

            for(int i = 0; i < contrasena.length(); i++) {
                if (condition.charAt(i) >= 97 && contrasenia.charAt(i) <= 122){
                    cuentaMinusculas += 1;
                } else if (contrasena.chartAt(i) >= 65 && contrasena.charAt(i) <= 90){
                   {else{
                    cuentaNumero += 1;
                   } 
                }
        }
System.out.printIn(cuentaNumero +)
if (cuentaNumero >= 4 && cuentaMinusculas > 0 && cuentaMayusculas > 1) return true | false ;

}
